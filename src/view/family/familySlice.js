import { createSlice } from '@reduxjs/toolkit'

export const familySlice = createSlice({
  name: 'familySlice',
  initialState: {
    pic: []
  },
  reducers: {},
  extraReducers: (builder) => {
    builder.addCase(

    )
  }
})